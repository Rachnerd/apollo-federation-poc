{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "start": "lerna run start --stream",
    "gateway": "lerna run --scope gateway start --stream",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "prettier": "^1.18.2"
  }
}
